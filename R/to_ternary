# 3^3 3^2 3^1 3^0
#  27   9   3   1


to_ternary <- function(n) {
    result = integer()
    while (n > 0) {
        result <- c(n %% 3, result)
        n <- n %/% 3
    }
    paste(result, collapse = "")
}

to_ternary(25) == "221"
to_ternary(87) == "10020"
